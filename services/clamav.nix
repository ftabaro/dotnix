{
  clamav = {
    daemon = {
      enable = true;
      # settings = {};
    };
    updater = {
      enable = true;
      frequency = 1; # number of checks per day
      # settings = {};
    };
  };
}
